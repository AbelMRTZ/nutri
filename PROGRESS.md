# Estado del proyecto Nutri

> Este archivo se actualiza al final de cada bloque de trabajo y se lee automáticamente
> al arrancar cualquier sesión de Claude Code en este repo (importado desde `CLAUDE.md`).
> Sirve para que una sesión nueva retome el trabajo sin perder contexto.

Última actualización: **2026-09-09**.

## Visión general de fases

| Fase | Estado |
|---|---|
| Fase 1 — Auth, onboarding, shell, sistema de diseño "Vibrante/Enérgico" | ✅ Completa y commiteada |
| Fase 2 — Despensa (Alimentos, Comidas, Planes) | ✅ Completa y commiteada |
| Fase 3 — Calendario, bloque 1 (asignar/quitar planes a días, marcar "Libre") | ✅ Completa y commiteada |
| Fase 3 — Calendario, bloque 2 (seguimiento de consumo + puntuación diaria) | ✅ Completa y commiteada |
| Fase 3 — Despensa → Planes: sustitución inteligente de alimentos | ✅ Completa y commiteada (ver nota de alcance abajo) |
| Fase 3 — Calendario, bloque 3 (programación/repetición de planes) | ✅ Completa y commiteada — **Fase 3 cerrada por completo** |
| Fase 4 — Entrenamiento, bloques 1-3 (catálogo de Ejercicios + Rutinas) | ⛔ **Rehecho por completo (2026-09-12)** — no coincidía con el encargo real, ver sección al final |
| Fase 4 — Entrenamiento rehecho: actividades con gasto calórico por día | ⚠️ Código completo, **migración pendiente de aplicar** (MCP de Supabase desconectado) — ver sección al final |

> **Nota de alcance:** el plan original agrupaba "sustitución inteligente" bajo Calendario, pero
> la funcionalidad opera sobre `plan_item_foods` (la instantánea editable de un plan), no sobre
> `calendar_days` — así que vive en `src/features/plans/`, no en `src/features/calendar/`. Se
> mantiene aquí bajo "Fase 3" solo para no romper el orden de encargo original.

Commits hasta ahora (los más recientes primero):
```
(pendiente) Phase 4 / slice 3: workout session tracking (closes Phase 4)
624e2aa Phase 4 / slice 2: Calendario <-> Rutinas assignment
471c21e Phase 4 / slice 1: Entrenamiento — Ejercicios + Rutinas catalog
39398f5 Phase 3 / slice 3: Calendario → plan scheduling/repetition (closes Phase 3)
3eb77f0 Phase 3: Despensa → Planes — smart food substitution
2cb5194 Phase 3 / slice 2: Calendario → daily consumption tracking + score
6a61e41 Phase 3 / slice 1: Calendario (assign/remove plans per day, free days)
8f575a3 Add project status handoff + Calendario slice 1 prep work
e8fe14a Phase 2 / slice 3: Despensa → Planes
7ef6894 Phase 2 / slice 2: Despensa → Comidas
d0e99bb Phase 2 / slice 1: Despensa → Alimentos
ff58fc3 Apply "Vibrante/Enérgico" as the app's permanent design system
812732b Phase 1: auth, personalization onboarding, and app shell
```

## Qué hay ya construido (Fases 1-4 completas)

Arquitectura de referencia — leer el código es más fiable que resumirlo aquí, pero como mapa rápido:

- `src/features/<feature>/` por cada dominio (`auth`, `profile`, `foods`, `meals`, `plans`, `calendar`, `training`, `pantry`, `home`, `settings`, `app-shell`), cada uno con `schema.ts` (zod + opciones/labels) si hace falta, `api/` (wrappers finos de Supabase, lanzan en error), `hooks/` (TanStack Query, una función por hook, claves `xQueryKey(...)`), `mappers.ts` si hace falta, `components/`, `index.ts` (barrel — las features solo se importan entre sí a través de él, nunca con rutas internas, **excepto** los módulos de cálculo puro tipo `contribution.ts`/`totals.ts`/`score.ts`/`substitution.ts`/`calories.ts`, que se importan por su ruta concreta *entre módulos de cálculo* para no arrastrar dependencias nativas a los tests de Jest — un componente de UI sí puede importarlos vía barrel normalmente, como hace `FoodSubstitutionPickerScreen` con `suggestSubstitutes` desde `@/features/foods`). `pantry` es un feature "hub" mínimo (solo la pantalla de menú de Despensa); `training` ya no lo es (ver sección final — pasó de un hub de menú a ser la propia pantalla de calendario de actividades). *(`exercises`/`routines` existieron como features hasta el rediseño completo de Entrenamiento del 2026-09-12 — eliminadas por completo, ver esa sección.)*
- `src/components/`: primitivas compartidas (`Screen`, `ThemedText`, `Button`, `TextField`, `NumericField`, `OptionPicker`, `LabeledSlider`, `FieldLabel`, `PlaceholderScreen`, `FullScreenSpinner`, `ConfirmDialog`, `CollapsibleSection`, `MacroDonutChart`, `TargetProgressBar`).
- `src/constants/theme.ts` + `src/hooks/use-theme.ts`: identidad visual "Vibrante/Enérgico" (negro casi puro estructural, coral `accent` para selección, lima `accentSecondary` para progreso, radios 10-14px, bordes sólidos 1.5px, tipografías Space Grotesk/Work Sans).
- `src/hooks/use-themed-stack-options.ts`: opciones de cabecera compartidas para cualquier `Stack`.
- `src/lib/dates.ts`: helpers de fecha sin librería externa (`toDateKey`/`fromDateKey`, `addDays`, `isSameDay`, `isToday`, `formatChipWeekday`, `formatDisplayDate` vía `Intl.DateTimeFormat('es-ES', ...)`).
- Patrón de rutas: cada sección de una tab que necesita varias pantallas se convierte en un directorio con su propio `_layout.tsx` (Stack), y la tab padre (`(tabs)/_layout.tsx`) pasa esa entrada a `headerShown:false` — ver `despensa/_layout.tsx` → `despensa/comidas/_layout.tsx` → `despensa/planes/_layout.tsx`, `calendario/_layout.tsx` y ahora `entrenamiento/_layout.tsx` → `entrenamiento/ejercicios/_layout.tsx` / `entrenamiento/rutinas/_layout.tsx` como ejemplos ya construidos.
- Base de datos: `supabase/migrations/0001` a `0010` — `profiles`, `foods`, `meals`+`meal_items`, `plans`+`plan_items`+`plan_item_foods`, `calendar_days`, `plan_item_completions`, `exercises`+`routines`+`routine_exercises`, `routine_exercise_completions`. La sustitución de alimentos y la programación de planes **no añadieron migración** (reutilizaron RLS/estructura existente). Convenciones generales: enums de Postgres (no text+check) para catálogos fijos; toda tabla propia de usuario tiene `user_id` + 4 políticas RLS (o resuelve propiedad vía `exists` contra su padre si no tiene `user_id` propio); una tabla hija exclusivamente propiedad de su padre usa FK `on delete cascade` (`meal_items.meal_id`, `plan_items.plan_id`, `plan_item_foods.plan_item_id`, `plan_item_completions.calendar_day_id`/`plan_item_id`, `routine_exercises.routine_id`, `routine_exercise_completions.calendar_day_id`/`routine_exercise_id`); una FK a una entidad-catálogo *reutilizable* (`food_id`, `meal_id`, `exercise_id`, `routine_id` en `calendar_days` como "ingrediente") usa RESTRICT (sin `on delete`) para que borrarla en uso lance `23503`, capturado por `src/lib/supabase/errors.ts` (`isForeignKeyViolation`/`friendlyDeleteErrorMessage`).
- `calendar_days` tiene una fila por `(user_id, date)` (constraint unique). `plan_id`/`is_free` son mutuamente exclusivos (check constraint) y representan el estado nutricional del día; `routine_id` (añadido en 0009) es un campo **independiente** en la misma fila — un día puede tener plan de comida Y rutina de entrenamiento a la vez, estar libre Y entrenar, etc., las 4 combinaciones son válidas. La ausencia de fila (o una fila con los tres campos vacíos) significa "sin planificar". Por eso las mutaciones de "quitar" en `calendar` (`useUpdateCalendarDay`) hacen **`update` de un campo concreto a `null`/`false`, nunca `delete` de la fila entera** — borrar la fila para quitar el plan borraría también una rutina asignada ese mismo día si la hubiera. `plan_item_completions` y `routine_exercise_completions` siguen una filosofía relacionada pero distinta: ahí la **existencia** de la fila es el flag "hecho" (insert-o-delete vía `useToggleMealCompletion`/`useToggleRoutineExerciseCompletion`, sin columna booleana ni `update`), porque esas tablas no comparten fila con nada más.
- Lección aprendida (arrastre): **no usar librerías de arrastre basadas en gestos** (se probó `react-native-draggable-flatlist`; su medición interna usa `findNodeHandle`, no soportado en RNW). Se sustituyó por botones de subir/bajar.
- Lección aprendida (RNW + `FlatList` horizontal): un `FlatList`/`ScrollView` horizontal **sin `style` con altura explícita** se estira para ocupar todo el alto disponible del contenedor flex en React Native Web (bien en iOS/Android). Fix: `style` con `height` fija + `flexGrow: 0, flexShrink: 0`. Comprobar siempre visualmente en `expo start --web`, no solo con `tsc`.
- Lección aprendida (React Compiler + react-hook-form, **importante y de aplicación general**): con `transform.reactCompiler=true`, **nunca desestructurar `formState: { errors }` directamente de `useForm()`/`useFormContext()`**. react-hook-form expone `formState` como un objeto envuelto en un Proxy que registra qué claves lee cada componente y lo re-renderiza directamente cuando cambian — pero el React Compiler no detecta esa lectura oculta como una dependencia reactiva y memoiza el render, así que un campo obligatorio vacío bloqueaba "Siguiente"/"Guardar" (el `trigger()`/resolver sí marcaba el error internamente) sin mostrar nunca el borde rojo ni el mensaje al usuario. "Funcionaba" por accidente en los campos que el propio usuario acababa de editar, porque el `Controller` de ESE campo fuerza su propio re-render por otra vía y de paso refresca sus hermanos — pero nunca en un campo intacto. Fix aplicado a los 11 formularios de la app: usar `const { errors } = useFormState({ control })` (import `useFormState` de `react-hook-form`) en vez de leer `formState` del objeto devuelto por `useForm`/`useFormContext` — devuelve un objeto realmente nuevo en cada cambio relevante, que el compiler sí trackea. **Cualquier formulario nuevo debe usar `useFormState({ control })` desde el principio**, nunca `formState` desestructurado.
- Verificación end-to-end de cada bloque: usuario de prueba creado por SQL directo (`auth.users`+`auth.identities`, confirmado sin email), Playwright contra `expo start --web`, borrado del usuario de prueba (y sus datos) al terminar. **Detalle importante:** insertar en `auth.users` a mano sin rellenar `email_change`, `email_change_token_new`, `email_change_token_current`, `phone_change`, `phone_change_token` y `reauthentication_token` (dejarlos en `NULL`) hace que el login por password devuelva `500` (el driver Go de GoTrue no puede escanear `NULL` en esas columnas `string`) — hay que ponerlas a `''` explícitamente; ya incorporado en la receta de creación de usuario de prueba usada en todos los bloques desde Calendario bloque 1. `tsc --noEmit`, `npx jest`, `npx expo lint` deben quedar limpios antes de dar un bloque por cerrado, y `get_advisors` (Supabase) debe devolver 0 avisos de seguridad nuevos tras cada migración que sí toque el esquema (el único aviso presente, `auth_leaked_password_protection`, es preexistente).
- Playwright no es una dependencia del proyecto (`node_modules`) pero está instalado globalmente vía nvm; para usarlo desde un script Node en este repo hace falta `NODE_PATH="$(npm root -g)" node script.js`.

## Qué hay construido en Calendario, bloque 1 (Fase 3)

- `src/features/calendar/`: `api/calendarDays.ts` (`getCalendarDay`, `listCalendarDaysInRange`, `upsertCalendarDay`), `hooks/` (`useCalendarDay`, `useCalendarDaysRange`, `useAssignPlanToDay`, `useMarkDayFree`), `components/` (`DayStrip`, `CalendarDayPanel`, `AssignedPlanSummary`, `CalendarScreen`, `PlanAssignmentPickerScreen` + `PlanPickerListItem`). *(Nota: el "quitar" original usaba `deleteCalendarDay`/`useRemoveDayAssignment`, borrando la fila entera; sustituido en el bloque 2 de Entrenamiento por `updateCalendarDay`/`useUpdateCalendarDay` — ver más abajo por qué.)*
- Rutas: `calendario/_layout.tsx`, `calendario/index.tsx`, `calendario/asignar-plan.tsx`.
- Verificado end-to-end: asignar plan a un día, "Editar" navega a `despensa/planes/[id]`, "Quitar" con `ConfirmDialog`, "Marcar como libre"/"Quitar" sobre día libre, y borrar desde Despensa → Planes un plan asignado a un día muestra el aviso amigable "en uso" en vez de un error crudo.

## Qué hay construido en Calendario, bloque 2 (Fase 3) — seguimiento + puntuación

- `supabase/migrations/0007_plan_item_completions.sql`: tabla `plan_item_completions`.
- `src/features/calendar/calculations/score.ts` (+ test): `calculateDailyScore(totals, targets)` pondera calorías 40% y cada macro 20%, suelo en 0, `null` si falta cualquier target.
- `src/features/calendar/api/planItemCompletions.ts` + hooks `useCalendarDayCompletions`/`useToggleMealCompletion`.
- Componentes: `MealCompletionList`, `DailyScoreSummary` (reutiliza `PlanProgressSummary`/`TargetProgressBar` de `plans`, exportados desde `plans/index.ts` junto con `PlanTargets` y `PlanItemWithDetails`), `DailyTrackingSection`.
- Verificado end-to-end con datos sembrados por SQL: la puntuación calculada a mano coincidía exacto con la mostrada en pantalla (51/100) al marcar una comida.

## Qué hay construido en sustitución inteligente de alimentos (Fase 3, en Despensa → Planes)

- `src/features/foods/calculations/substitution.ts` (+ `substitution.test.ts`, módulo puro): `suggestSubstitutes(target, candidates)` — para cada alimento candidato calcula la cantidad que reproduce las mismas calorías que `target` (una `FoodContribution` ya calculada con `calculateFoodContribution`), y puntúa 0-100 la similitud de macros (proteína/carbohidratos/grasa a partes iguales) a esa cantidad igualadora. Así "pollo → pavo" puntúa por encima de "pollo → aceite de oliva" aunque ambos puedan dosificarse para dar las mismas calorías. Candidatos con calorías-por-ración ≤ 0 (p. ej. agua) se descartan porque no se pueden escalar a ningún objetivo positivo. Exportado desde `src/features/foods/index.ts` (`suggestSubstitutes`, tipo `SubstitutionSuggestion`) para que las pantallas lo consuman vía barrel normalmente.
- `src/features/plans/api/planItemFoods.ts`: nueva `substitutePlanItemFood(id, foodId, quantity)` — un `update` de `food_id`+`quantity` sobre la fila de `plan_item_foods`; **sin migración**, la policy `plan_item_foods_update_own` ya cubría el nuevo `food_id` (debe pertenecer al usuario). Hook `useSubstitutePlanItemFood`.
- `FoodSubstitutionPickerScreen` (en `plans`, ruta `despensa/planes/sustituir-alimento.tsx`, parámetros `planId`+`planItemFoodId`): localiza el `plan_item_food` actual vía `usePlanItems(planId)`, calcula su `FoodContribution` como objetivo, y lista `useFoods(userId)` (menos el alimento actual) ordenados por similitud, con buscador y filtro de categoría igual que `FoodPickerScreen`/`MealPickerScreen`. Cada fila (`FoodSubstitutionListItem`) muestra alimento, cantidad sugerida, kcal resultante y % de similitud. Al elegir uno, sustituye y vuelve atrás.
- Disparador: icono "swap-horizontal-outline" añadido a cada fila de alimento en `PlanMealGroup.tsx` (`PlanItemFoodRow`), junto al icono de eliminar comida ya existente.
- Verificado end-to-end con datos sembrados por SQL (pollo 200kcal/100g como objetivo a 150g, pavo 150kcal/100g y aceite de oliva 884kcal/100g como candidatos): el pavo salió primero (73% similitud) y el aceite segundo (48%), y sustituir por pavo dejó el plan con "Pavo E2E · 200 g" y los totales de proteína/grasa del plan recalculados correctamente.

## Qué hay construido en programación/repetición de planes (Fase 3, cierra Calendario)

- `src/features/calendar/calculations/schedule.ts` (+ test, módulo puro): `generateScheduleDates(startDate, days, weekdays)` — cada fecha en la ventana `[startDate, startDate+days-1]` cuyo `Date#getDay()` esté en el set `weekdays`. No hay tabla de "reglas recurrentes": la programación **materializa filas `calendar_days` inmediatamente** (bulk upsert) en vez de guardar una regla abstracta a interpretar después — más simple y consistente con que `calendar_days` sea la única fuente de verdad del calendario.
- `src/features/calendar/api/calendarDays.ts`: nueva `upsertCalendarDays(rows[])` (versión en bloque de `upsertCalendarDay`, mismo `onConflict: 'user_id,date'`). Hook `useApplyPlanSchedule`.
- `PlanScheduleScreen` (en `calendar`, ruta `despensa/planes/programar-calendario.tsx`, parámetro `planId` — mismo patrón cruzado que `calendario/asignar-plan.tsx` usando una pantalla de `plans`, pero al revés): siempre empieza hoy, con un `NumericField` "repetir durante (días)" y 7 chips de día de la semana (L M X J V S D, todos seleccionados por defecto). Antes de aplicar, calcula conflictos contra `useCalendarDaysRange` (días que ya tienen plan o están libres) y si hay alguno, pide confirmación con `ConfirmDialog` antes de sobrescribir; si no hay conflicto, aplica directo.
- Disparador: botón "Programar en calendario" en `PlanDetailScreen`, justo debajo de `PlanProgressSummary`.
- Verificado end-to-end con datos sembrados por SQL (un día ya marcado libre dentro de la ventana): el diálogo de conflicto apareció con el recuento correcto (1), y tras confirmar, ese día pasó de "Día libre" a mostrar el plan programado.

## ⛔ SUSTITUIDO POR COMPLETO (2026-09-12) — ver "Rehecho: Entrenamiento pasa a ser actividades con gasto calórico" al final del archivo. Las tablas/features/rutas descritas en los tres bloques siguientes (`exercises`, `routines`, `routine_exercises`, `routine_exercise_completions`, `calendar_days.routine_id`) ya no existen.

## Qué hay construido en Entrenamiento, bloque 1 (Fase 4) — catálogo de Ejercicios + Rutinas

Decisión tomada con el usuario (2026-09-07): el primer bloque de Entrenamiento es un catálogo de Ejercicios + Rutinas en espejo de Despensa (Alimentos → Comidas), **sin tocar todavía el Calendario** — sin tercer nivel tipo "Planes" que asigne rutinas a días; eso queda para un bloque futuro de integración.

- `supabase/migrations/0008_exercises_routines.sql`: enums `exercise_muscle_group` (chest/back/shoulders/arms/legs/core/cardio/full_body/other) y `exercise_equipment` (bodyweight/free_weights/machine/bands/cardio_machine/other); tablas `exercises` (mismo patrón que `foods`), `routines` (mismo patrón que `meals`), y `routine_exercises` (un ejercicio dentro de una rutina, con `sets`/`reps`/`weight_kg` y `sort_order`).
- **Divergencia deliberada de `meal_items`:** `meal_items` es solo remove-and-re-add (comentario explícito en 0004: "a quantity change is a remove-and-re-add, not an edit-in-place"), pero `routine_exercises` sí soporta **edición in-place** de `sort_order` (reorder, como `plan_items`) y de `sets`/`reps`/`weight_kg` — la progresión de carga (subir el peso al mismo ejercicio semana a semana) es un flujo tan frecuente que forzar quitar-y-reañadir sería fricción real, no solo una simplificación razonable. `exercise_id` es RESTRICT (like `food_id`/`meal_id`) porque `exercises` es un catálogo reutilizable independiente; `routine_id` es CASCADE porque un `routine_exercise` solo existe dentro de una rutina.
- `src/features/exercises/`: mismo esqueleto que `foods` (schema con las dos enums + `exerciseFormSchema`, api CRUD, hooks, `ExerciseForm`/`ExerciseListItem`/`ExercisesListScreen`/`CreateExerciseScreen`/`EditExerciseScreen`/`EjerciciosHeaderActions`).
- `src/features/routines/`: mismo esqueleto que `meals` para la rutina en sí (`RoutineForm`/`RoutineListItem`/`RoutinesListScreen`/`CreateRoutineScreen`/`RutinasHeaderActions`), más `RoutineDetailScreen` (reutiliza el patrón reorder-por-botones de `PlanDetailScreen`, con su propio `swap` local) y `RoutineExerciseRow` (tarjeta con botones subir/bajar + tres `TextInput` compactos para editar series/reps/kg in-place, mismo patrón que la cantidad editable de `PlanItemFoodRow` pero con tres campos). `ExercisePickerScreen`+`ExercisePickerListItem` mirror `FoodPickerScreen` de `meals` (buscador + filtro por grupo muscular). Añadir un ejercicio inserta con valores por defecto (3 series, 10 reps, sin peso) vía `useAddExerciseToRoutine` — el usuario ajusta los valores in-place después, sin pantalla de configuración intermedia.
- `src/features/training/`: feature hub mínimo, solo `EntrenamientoHomeScreen` (menú con tarjetas "Ejercicios"/"Rutinas"), igual que `pantry` para Despensa.
- Rutas: se borró el placeholder plano `entrenamiento.tsx` y se creó `entrenamiento/_layout.tsx` (index con `TopBar`, `ejercicios`/`rutinas` con `headerShown:false`) + los dos sub-stacks (`entrenamiento/ejercicios/_layout.tsx`, `entrenamiento/rutinas/_layout.tsx` con su `agregar-ejercicio.tsx`). Cambiado `entrenamiento` a `headerShown:false` en `(tabs)/_layout.tsx`, igual que `despensa`/`calendario`.
- Verificado end-to-end con Playwright (usuario de prueba por SQL): crear dos ejercicios, crear una rutina, añadir ambos ejercicios, editar series/reps/peso in-place (persiste tras recargar), reordenar con los botones subir/bajar (orden confirmado antes/después), intentar borrar un ejercicio todavía en uso en una rutina (aviso amigable "en uso" correcto, bloqueado), quitarlo de la rutina y borrarlo con éxito, y borrar la rutina entera.

## Qué hay construido en Entrenamiento, bloque 2 (Fase 4) — integración con Calendario

- `supabase/migrations/0009_calendar_routine_assignment.sql`: añade `calendar_days.routine_id` (nullable, RESTRICT — mismo motivo que `plan_id`) y sustituye las policies `calendar_days_insert_own`/`calendar_days_update_own` para verificar también que un `routine_id`, si está presente, pertenezca al usuario (mismo patrón que ya existía para `plan_id`). **Decisión de diseño clave:** `routine_id` vive en la misma fila que `plan_id`/`is_free` en vez de en una tabla nueva, porque es el mismo día — pero es un campo ortogonal, no mutuamente exclusivo con nada: comer "libre" no implica saltarse el entrenamiento, ni un día de dieta implica entrenar. Esto obligó a un cambio de fondo: las mutaciones de "quitar" ya no pueden borrar la fila entera (harían desaparecer una rutina asignada al quitar el plan, o viceversa), así que `useRemoveDayAssignment`+`deleteCalendarDay` (borraban la fila) se sustituyeron por `useUpdateCalendarDay`+`updateCalendarDay` (limpian un campo concreto a `null`/`false`).
- `useAssignRoutineToDay` reutiliza `upsertCalendarDay` tal cual (pasando solo `{user_id, date, routine_id}`) — el upsert de PostgREST solo toca en el `ON CONFLICT DO UPDATE` las columnas presentes en el payload, así que asignar una rutina nunca pisa un `plan_id`/`is_free` ya existente en esa fila, y viceversa (ya se confirmó este comportamiento también en `useAssignPlanToDay`/`useMarkDayFree`, que tampoco tocan `routine_id`).
- `CalendarDayPanel` ahora tiene dos secciones independientes — "Plan del día" (lógica sin cambios de fondo, solo re-cableada a `useUpdateCalendarDay`) y "Entrenamiento del día" (mismo patrón: sin planificar / `AssignedRoutineSummary` con Editar+Quitar). Un único `ConfirmDialog` compartido con estado `pendingRemoval: 'plan' | 'free' | 'routine' | null` en vez de tres diálogos.
- Nuevos componentes en `calendar`: `AssignedRoutineSummary`, `RoutinePickerListItem`, `RoutineAssignmentPickerScreen` (ruta `calendario/asignar-rutina.tsx`) — mismo patrón que sus equivalentes de `plans`.
- `DayStrip` ahora pinta hasta dos puntos de indicador por día (nutrición: lima=libre/coral=plan; entrenamiento: punto oscuro si hay rutina asignada), en vez de uno solo.
- Los mensajes de error amigable de `RoutineListItem`/`RoutineDetailScreen` (ya escritos en el bloque 1, pero nunca alcanzables porque no existía ninguna FK hacia `routines`) ahora sí se activan: "Esta rutina está asignada a un día del calendario y no se puede eliminar."
- Verificado end-to-end con Playwright: asignar plan y rutina al mismo día, quitar solo la rutina (el plan permanece), quitar solo el plan (la rutina permanece), marcar otro día como libre y asignarle una rutina (ambas conviven, confirmado visualmente con los dos puntos de indicador en el día strip), e intentar borrar una rutina todavía asignada a un día libre (bloqueado con el aviso amigable).

## Qué hay construido en Entrenamiento, bloque 3 (Fase 4) — seguimiento de sesiones realizadas

- `supabase/migrations/0010_routine_exercise_completions.sql`: tabla `routine_exercise_completions`, calco exacto de la filosofía de `plan_item_completions` (la existencia de la fila es el flag "hecho", cascade en ambos lados porque un `routine_exercise` solo existe dentro de una rutina). La policy de insert verifica que el `routine_exercise_id` pertenezca a la rutina actualmente asignada a ese `calendar_day` (mismo patrón defensivo que `plan_item_completions`).
- `src/features/calendar/api/routineExerciseCompletions.ts` + hooks `useCalendarDayRoutineCompletions`/`useToggleRoutineExerciseCompletion` — mismo patrón 1:1 que sus equivalentes de nutrición.
- Componentes: `RoutineExerciseCompletionList` (checklist con tachado, mismo patrón que `MealCompletionList` pero mostrando "series x reps @ peso" en vez de kcal) y `DailyTrainingSection` (junta `useRoutineExercises` + los hooks de completions, muestra el checklist y un resumen "X de Y ejercicios completados"). **Sin puntuación 0-100** a diferencia de nutrición: no hay un equivalente natural a "macros objetivo" para un entrenamiento — completar o no cada ejercicio ya es la señal completa, así que un recuento simple es más honesto que inventar una fórmula de puntuación arbitraria.
- `CalendarDayPanel`: la sección "Entrenamiento del día" ahora muestra `AssignedRoutineSummary` + `DailyTrainingSection` cuando hay una rutina asignada (antes solo mostraba el resumen).
- Verificado end-to-end con datos sembrados por SQL (una rutina con un ejercicio asignada a hoy): marcar el ejercicio actualiza el resumen a "1 de 1", persiste tras recargar la página, y desmarcar lo vuelve a "0 de 1".

## Estado final

**Fase 4 (Entrenamiento) queda cerrada por completo, y con ella el encargo original completo: Fases 1-4 terminadas.** Cualquier trabajo futuro (mejoras, nuevas fases, pulido) parte de una base ya completa y verificada end-to-end en cada bloque.

## Pasada de pulido post-encargo (2026-09-08)

Revisión general sin nuevo alcance funcional, más allá de cerrar un placeholder de Fase 1 que ya tenía todo su soporte construido:

- Barrido de calidad: `grep` de `TODO`/`FIXME`, `console.log`, `as any`/`@ts-ignore`/`eslint-disable` en todo `src/` — limpio salvo un `eslint-disable-next-line react-hooks/exhaustive-deps` preexistente de Fase 1 (`MacrosReviewStep.tsx`), ya justificado con comentario y correcto tal cual está.
- **`InicioScreen` dejó de ser un placeholder**: ahora renderiza `CalendarDayPanel` (exportado desde el barrel de `calendar`) con `date = hoy`, es decir, exactamente el resumen que el placeholder original prometía ("progreso de calorías, macros y tu plan de hoy") — cero código nuevo, solo conectar una pantalla ya construida y verificada. Verificado visualmente con Playwright: la pestaña Inicio muestra ahora "Plan del día" + "Entrenamiento del día" de hoy, con todos sus controles (asignar/quitar, checklists, puntuación).
- **Bug de bajo impacto corregido**: los checklists de completions (`MealCompletionList`, `RoutineExerciseCompletionList`) no bloqueaban un doble-tap rápido mientras la mutación de toggle estaba en curso — un segundo toque sobre la misma fila antes de que la primera mutación asentara podía encolar un insert/delete duplicado contra la constraint `unique(calendar_day_id, plan_item_id|routine_exercise_id)`, fallando silenciosamente (el error queda en el estado de la mutación, no se propaga, pero el segundo toggle no surtía efecto visual). Fix: nueva prop `pendingId` (calculada como `mutation.isPending ? mutation.variables?.id : undefined` en `DailyTrackingSection`/`DailyTrainingSection`) que deshabilita solo la fila en vuelo, sin bloquear el resto de la lista. Verificado con Playwright disparando dos clics consecutivos sin esperar entre medias: sin errores de consola, estado final correcto.
- **No se tocaron** `AjustesScreen` (preferencias de la app — unidades, notificaciones, apariencia) ni `despensa/investigar.tsx` (Investigar Alimentos): ambos son placeholders explícitos de funcionalidad **fuera del alcance del encargo original** (no aparecen en ninguna de las 4 fases documentadas), no vacíos a rellenar. `AjustesScreen` es distinto de `CuentaScreen` (`/(app)/cuenta.tsx`), que sí es funcional desde Fase 1 (perfil + cerrar sesión).
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios tras los cambios; sin migraciones nuevas (no se tocó el esquema).

## Pruebas manuales del usuario — Fase 1 (2026-09-09)

El usuario probó Fase 1 a mano contra un checklist y reportó varios bugs reales, corregidos en dos tandas (commits `20bfe7c`, `41c1a5a`, `5cfcf33`): feedback de confirmación de email pendiente en el registro, mensajes de error de auth en español, un glitch de outline CSS, un bug crítico de RHF donde un campo numérico no se podía borrar del todo tras volver a un paso anterior del onboarding (root cause: un `Controller` de react-hook-form, tras desmontarse/remontarse, revertía silenciosamente un valor puesto a `undefined` — arreglado manteniendo todos los pasos del wizard siempre montados, alternando visibilidad con `display:none` en vez de renderizado condicional), fusión de "Perder peso"/"Ganar peso" en una sola opción "Ganar/Perder peso" (deriva la dirección real comparando peso objetivo vs. actual), recálculo automático de macros al cambiar el objetivo en un paso anterior, un botón para cancelar el onboarding, y **el bug de fondo ya documentado arriba de React Compiler + `formState` de react-hook-form** (validación bloqueaba "Siguiente" sin mostrar nunca el error al usuario) — corregido en los 11 formularios de la app de una vez, no solo en onboarding.

## Pruebas manuales del usuario — Fase 2, Despensa → Alimentos (2026-09-09)

Peticiones del usuario tras probar el listado de Alimentos, todas de UX (sin bugs de fondo esta vez):

- **Separar "ver" de "editar"**: pulsar un alimento ya no navega directo a un formulario de edición (idéntico al de creación, confuso). Ahora `alimentos/[id]` es una pantalla de **detalle de solo lectura** (`FoodDetailScreen`) con el mismo lenguaje visual que la pantalla de "añadir alimento a una comida" (`QuantityScreen` de `meals`): donut de macros + chips de nutrientes opcionales, calculados con `calculateFoodContribution(food, food.serving_type === 'per_100g' ? 100 : 1)` (es decir, los valores "por ración" tal cual están guardados, sin escalar a una cantidad). Un botón "Editar alimento" lleva a la nueva ruta `alimentos/editar/[id]`, que renderiza el `EditFoodScreen` de siempre sin cambios. **Refactor de paso**: `NutrientChipsRow` vivía en `features/meals/` pero solo depende de `FoodContribution` (un concepto de `foods`) — se movió a `features/foods/components/` y se exporta desde el barrel de `foods`, para que tanto `QuantityScreen` (meals) como `FoodDetailScreen` (foods) lo consuman sin crear un ciclo de imports entre barrels de features.
- **Buscador + filtro de categoría en el listado**: `FoodsListScreen` gana el mismo patrón ya usado en `FoodPickerScreen` (el selector de alimentos al añadir a una comida) — `TextField` de búsqueda por nombre + filtro de categoría, filtrando con `useMemo` sobre `foods`. Si el filtro no encuentra nada se muestra "No se han encontrado alimentos." en vez de la pantalla vacía grande (esa queda solo para cuando de verdad no hay ningún alimento creado). *(El filtro de categoría en sí se rehizo justo después — ver el siguiente punto — así que "con 'Todas'" ya no aplica, se sustituyó por selección múltiple.)*
- **Mensaje de error "en uso" en una caja con auto-dismiss**: nuevo componente compartido `src/components/error-banner.tsx` (`ErrorBanner`) — rectángulo con borde y fondo tintado en `theme.danger`, que se autodesmonta a los 5 segundos (`setTimeout` en un `useEffect` que solo depende del texto del mensaje, no de los callbacks, para no reiniciar el temporizador en cada re-render del padre). Sustituye el `ThemedText` suelto que mostraba "Este alimento está en uso y no se puede eliminar." tanto en `FoodListItem` (listado) como en `EditFoodScreen` (pantalla de edición) — mismo componente reutilizable si hiciera falta en otro sitio de la app más adelante.
- Verificado end-to-end con Playwright (usuario + dos alimentos + una comida sembrados por SQL, uno de los alimentos en uso en esa comida): búsqueda por nombre y por categoría filtran correctamente: pulsar un alimento muestra el detalle (no un formulario), "Editar alimento" lleva al formulario de edición real, y borrar el alimento en uso muestra el rectángulo de error que sigue visible justo después y ya no está tras esperar 5.5s.
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios; sin migraciones nuevas.

## Pruebas manuales del usuario — filtro de categoría multi-selección + plegable (2026-09-09)

Petición de seguimiento inmediata sobre el filtro de categoría recién añadido: debía admitir **varias categorías a la vez** (toggle individual, no una sola selección excluyente) y **poder ocultarse** porque el `OptionPicker` de categorías, siempre desplegado, ocupaba mucho espacio vertical antes de llegar a los resultados — y pedía el mismo comportamiento en **ambos sitios**: el listado de Alimentos y el selector de alimentos al añadir uno a una comida.

- Nuevo `CategoryFilterSection` (`src/features/foods/components/CategoryFilterSection.tsx`, exportado desde el barrel de `foods`): chips de categoría con selección múltiple (`selected: FoodCategory[]`, `onToggle(category)` añade/quita esa categoría del array — sin opción "Todas", un array vacío ya significa "sin filtrar, mostrar todo"), envueltos en el `CollapsibleSection` ya existente (colapsado por defecto). El título muestra un contador — `Categoría (2)` — cuando hay categorías activas, para que se sepa que el filtro sigue aplicado incluso con la sección plegada.
- Sustituye el `OptionPicker` de categoría (selección única) tanto en `FoodsListScreen` como en `FoodPickerScreen` (`meals`) — mismo componente en los dos sitios, así que cualquier cambio futuro al filtro de categorías se hace una sola vez. `FoodCategory` (el tipo del enum de categoría) se exporta ahora desde `schema.ts`/el barrel de `foods` en vez de inferirse ad-hoc en cada sitio que lo necesitaba.
- Verificado end-to-end con Playwright en ambas pantallas (cuatro alimentos de categorías distintas sembrados por SQL): la sección aparece plegada al entrar, se expande al pulsar el título, seleccionar dos categorías (p. ej. Carne + Pescado) muestra ambas a la vez y el contador pasa a "(2)", quitar una las deja en "(1)", y plegar la sección de nuevo no borra el filtro (los resultados siguen filtrados con la sección cerrada) — igual en el listado de Alimentos y en el selector de "añadir alimento a una comida".
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios; sin migraciones nuevas.

## Pruebas manuales del usuario — `ErrorBanner` pasa a ser un toast flotante (2026-09-09)

El `ErrorBanner` de "Este alimento está en uso" recién añadido se renderizaba **inline** (en `FoodListItem`, empujando las filas de debajo de la lista; en `EditFoodScreen`, dentro del footer del formulario) — el usuario pidió que en vez de eso flotara **por encima** de lo que ya hay en pantalla, sin mover ningún componente, pegado a la parte baja justo por encima del menú de tabs, y que desapareciera sin dejar hueco tras los 5 segundos.

- `ErrorBanner` (`src/components/error-banner.tsx`) ahora se autoposiciona con `position: 'absolute', left:0, right:0, bottom:16` (más `zIndex`/`elevation` y una sombra) en vez de fluir dentro del layout normal. Como en React Native los elementos `absolute` se posicionan respecto a su padre inmediato (no busca un ancestro "positioned" como en CSS web), **debe renderizarse como hermano directo del contenedor `flex:1` de la pantalla**, nunca anidado dentro de una fila de lista o de un `ScrollView` — así "bottom" mide contra toda el área de la pantalla, no contra una región interna con scroll/recortada. Como el tab bar de abajo (`(tabs)/_layout.tsx`) no usa `position:'absolute'`, ya ocupa su propio espacio en el layout — el área de contenido de la pantalla ya termina justo por encima de él, así que "bottom:16" cae naturalmente justo encima del menú sin necesidad de calcular su altura real.
- **`FoodListItem` ya no gestiona su propio error**: recibe un callback `onDeleteError(message)` y se lo delega a `FoodsListScreen`, que es quien mantiene el estado y renderiza un único `ErrorBanner` a nivel de pantalla (como hermano de `Screen`, dentro de un `View` envolvente `flex:1`) — con filas dentro de un `FlatList`, un `ErrorBanner` anidado ahí se habría posicionado respecto a la fila, no respecto a la pantalla completa.
- `EditFoodScreen` sigue el mismo patrón: el `ErrorBanner` sale del `footer` del formulario (donde empujaba el botón "Eliminar alimento") y pasa a ser hermano de `Screen` dentro de un `View` `flex:1` envolvente.
- Verificado con Playwright (usuario + alimento en uso en una comida, sembrados por SQL): la posición de la fila del alimento antes y después de que aparezca el error es **idéntica** (no se desplaza nada), el aviso aparece justo por encima de la etiqueta del tab bar, y a los 5,5s ya no está — sin dejar hueco. Repetido también en `EditFoodScreen`, donde se solapa visualmente sobre el botón "Eliminar alimento" (esperado — "por encima de lo que haya").
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios; sin migraciones nuevas.

## Catálogo de alimentos USDA (Foundation Foods + SR Legacy) — nueva pieza, no una fase del encargo original (2026-09-09)

Petición nueva del usuario, fuera de las 4 fases originales: una base de datos de alimentos genéricos **fiable y trazable** (nada inventado/estimado por IA, NULL nunca 0, cada valor con su fuente/ID/fecha de importación), usando exclusivamente USDA FoodData Central (Foundation Foods + SR Legacy, sin Branded Foods ni scraping), con el modelo preparado para añadir productos Mercadona por EAN más adelante (sin implementarlo aún). Condición añadida por el usuario tras aprobar el plan: excluir platos preparados y alimentos muy ligados a una marca/región, priorizando ingredientes básicos universales.

Precedió a la implementación una fase de **plan aprobado explícitamente por el usuario** (herramienta de Plan Mode) tras investigar a fondo el modelo de datos actual (`foods` es 100% privado por usuario, sin ningún precedente de tabla compartida/sin dueño en todo el esquema) y la estructura real de los CSV de USDA (descargados y analizados de verdad, no asumidos — ver detalles abajo).

- **Esquema nuevo, aditivo, sin tocar `foods`/`meal_items`/`plan_item_foods`** (`supabase/migrations/0011_food_catalog.sql` + `0012_food_catalog_allow_flagged_negative_amounts.sql`):
  - Enum `food_category` ampliado con `dairy`/`legumes`/`tubers`/`oils_fats` (aditivo y seguro; también disponible ya para los alimentos personales de cada usuario).
  - `nutrients`: diccionario de nutrientes extensible, clave estable `usda_nutrient_nbr` (el código histórico de USDA, no el `id` de superficie que cambia entre versiones del dataset).
  - `reference_foods`: un alimento del catálogo, **sin `user_id`** — la primera tabla de todo el esquema sin RLS "por usuario" (`select using (true)`, sin políticas de insert/update/delete: solo escribe el script de importación con la service role key). Guarda `source`/`source_id` (fdc_id)/`source_dataset`/`source_dataset_version`/`imported_at` para trazabilidad completa, `name_original` (verbatim, nunca editado) + `name_es` (nullable, capa de traducción manual), y `ean_barcode` reservado sin uso para Mercadona.
  - `reference_food_nutrients`: tabla EAV, fuente de verdad única de cada valor — `amount`+`unit`+`reference_basis`+`source`+`source_food_id`+`derivation_code`, con `unique(reference_food_id, nutrient_id)`. Un nutriente no reportado por USDA simplemente no tiene fila aquí — nunca `amount = 0`.
  - **Lección aprendida en vivo**: la migración 0011 puso un `check (amount >= 0)` pensando que un valor negativo era "dato corrupto". Al ejecutar la importación real, USDA reportó 10 valores de "Carbohydrate, by difference" ligeramente negativos en Foundation Foods (p. ej. -0.475) — un artefacto real y documentado de cómo se calcula ese nutriente (100 − proteína − grasa − agua − ceniza, que puede quedar por debajo de cero por redondeo), no un error. La constraint rechazaba justo el caso que el usuario pidió conservar marcado, no descartar. Se corrigió con `0012` (elimina la constraint) — la validación de "sospechoso" vive en el script (`is_flagged`/`flag_reason`, nunca en un `check` de base de datos que solo puede aceptar o rechazar).
- **Fuente de datos, verificada descargando los ZIP reales** (no asumida de memoria): Foundation Foods (release 2026-04-30) y SR Legacy (release final, abril 2018) desde `fdc.nal.usda.gov/download-datasets.html`, cada uno por separado — Branded Foods nunca se descarga. `food.csv` de Foundation incluye de fondo filas internas de laboratorio (`sub_sample_food`, `market_acquisition`...) que no son alimentos reales — solo cuentan las filas `data_type = 'foundation_food'` (469) / `'sr_legacy_food'` (7.793).
- **Filtro de alimentos básicos** (condición del usuario): exclusión por categoría USDA completa y explícita (`scripts/import-usda/mapCategory.ts` — 28 categorías reales de USDA mapeadas una a una, 9 marcadas `EXCLUDED`: Fast Foods, Restaurant Foods, Meals/Entrees/Side Dishes, Baby Foods, American Indian/Alaska Native Foods, Soups/Sauces/Gravies, Snacks, Branded Food Products Database, Alcoholic Beverages, Quality Control Materials) + lista curada de tokens de marca (`brandFilter.ts`, construida inspeccionando de verdad las descripciones reales — SR Legacy cuela marcas como QUAKER/NESTLE/KRAFT/HORMEL/POST/MALT-O-MEAL incluso en categorías por lo demás genéricas como "Breakfast Cereals" o "Sweets"). Resultado real: de 8.262 candidatos, quedan **6.447 alimentos genéricos** (461 Foundation + 5.986 SR Legacy) tras excluir 1.450 por categoría y 365 por marca.
- **Script de importación** (`scripts/import-usda/`, TypeScript vía `tsx`, nuevo `npm run import:usda`): parseo de CSV (`csv-parse`), mapeo de categoría, filtro de marca, validación (`validate.ts`: unidad desconocida, rango implausible solo para los ~7 nutrientes que se entienden bien, negativo — todo marca, nada descarta ni altera), y `upsert.ts` con lotes de 500 filas usando `upsert(...).select()` sobre las claves únicas de la migración → **idempotente de verdad**, comprobado ejecutándolo dos veces sobre los mismos CSV (recuentos idénticos en la segunda pasada: 6.447 alimentos, 529.485 filas de nutrientes, sin crecer). Usa un cliente Supabase propio para Node (`supabaseAdmin.ts`, con la service role key — nunca el cliente de la app, que trae polyfills de React Native) y `node --env-file=.env` en vez de añadir `dotenv`.
- **Traducción al español**: `translations.es.json` vacío por defecto (`{}`), mantenido a mano por `fdc_id` — nunca generado por IA ni traductor automático; mientras no exista traducción, `name_es` queda `null` y la app caería al nombre original en inglés.
- **Verificado de verdad, no solo "debería funcionar"**: comparación directa de dos alimentos (Alaska Pollock fdc_id 2768188, Kale raw fdc_id 168421) contra la API pública oficial de USDA (`api.nal.usda.gov/fdc/v1/food/<id>`) — coincidencia exacta en proteína/grasa/carbohidratos/energía/vitamina C; auditoría de que un nutriente ausente en el CSV origen (p. ej. "Energy" nutrient_nbr 208, que este pollock en concreto no reporta directamente) no tiene fila en `reference_food_nutrients`; `get_advisors` sin avisos nuevos tras las dos migraciones (relevante porque son las primeras tablas de lectura abierta de todo el esquema).
- **Fuera de alcance de este bloque, explícitamente**: no se tocó ningún componente de UI en este bloque — ver el bloque siguiente ("Integración UI del catálogo") para eso. Tampoco se ha implementado nada de Mercadona (el modelo ya lo admite: `reference_foods.source = 'mercadona'` + `ean_barcode`, sin usar todavía).
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios. Nuevas dependencias de desarrollo: `tsx`, `csv-parse` (ninguna va al bundle de la app). Nueva variable de entorno server-only `SUPABASE_SERVICE_ROLE_KEY` (en `.env`, gitignored, nunca en el cliente). `data/usda/` (los CSV originales descargados) añadido a `.gitignore` — no se commitean.

## Integración UI del catálogo USDA (2026-09-09)

Petición de seguimiento inmediata: hacer usable desde la app el catálogo poblado en el bloque anterior. Precedida de un segundo plan aprobado explícitamente por el usuario (Plan Mode) tras explorar cómo `meal_items`/`plan_item_foods` referencian `foods.id` (`not null`, RESTRICT) y confirmar que tocar ese contrato para enlazar en vivo con `reference_foods` habría exigido cambios en RLS, tipos y cálculo en media docena de sitios ya construidos y probados.

- **Decisión de arquitectura**: "Añadir a mis alimentos" **materializa una copia personal** en `foods` (no un enlace en vivo) — mismo patrón que apps de nutrición reales (MyFitnessPal, Cronometer). A partir de ahí el alimento es indistinguible de uno creado a mano para el resto de la app (Alimentos, añadir a una comida, sustitución) — **cero cambios** en `meal_items`, `plan_item_foods`, `FoodPickerScreen`, `calculateFoodContribution` ni `FoodSubstitutionPickerScreen`.
- `supabase/migrations/0013_foods_source_reference.sql`: columna nueva `foods.source_reference_food_id` (nullable, `on delete set null`, con índice) — pura trazabilidad hacia el alimento de origen, ninguna política RLS de `foods` cambia.
- **Hallazgo real que cambió el diseño de la copia**: comprobado contra los CSV (no asumido), 91-92 de los 469 Foundation Foods (≈1,4% del catálogo total; SR Legacy 0 de 7.793) no reportan energía y/o algún macro completo. Como `foods.energy_kcal`/`fat_g`/`protein_g`/`carbs_g` son `not null`, esos alimentos **no se pueden copiar sin inventar un valor** — y no se inventa: el botón "Añadir a mis alimentos" se deshabilita para ellos con una explicación (verificado en vivo con "Oil, peanut", que no reporta proteína).
- `src/features/food-catalog/nutrientMapping.ts`: mapeo `nutrient_nbr → columna de foods` verificado contra el `nutrient.csv` real (no de memoria) — `energy_kcal` usa el primero disponible entre 208 (kcal) → 268 (kJ, convertido — conversión de unidad, no estimación) → 957/958 (factores de Atwater que USDA ya publica). `salt_g`, `omega3_g` y `vitamin_b8_mcg` no tienen nutriente USDA equivalente directo y quedan `null` al copiar, siempre — nunca una conversión inventada (p. ej. sodio→sal).
- Resto de la feature (`src/features/food-catalog/`): `api/referenceFoods.ts` (búsqueda server-side — 6.447 filas es demasiado para filtrar en cliente como hace el picker de alimentos personales), `hooks/` (`useSearchReferenceFoods` con debounce manual de 300ms — no existía ninguna utilidad de debounce en el repo —, `useReferenceFood`, `useReferenceFoodNutrients`, `useAddReferenceFoodToFoods` — la mutación "materializar", que reutiliza la copia si el usuario ya la había añadido antes en vez de duplicarla), `components/` (`CatalogSearchScreen` reutiliza `CategoryFilterSection` tal cual; `ReferenceFoodDetailScreen` reutiliza `MacroDonutChart`/`NutrientChipsRow`, con "N/D" en vez de un cero inventado cuando falta un macro concreto).
- **Bug real encontrado y corregido durante la verificación**: el filtro `.or()` de PostgREST usa la coma como separador de condiciones — como casi todos los nombres de USDA llevan coma ("Oil, peanut", "Broccoli, raw"...), la búsqueda devolvía silenciosamente cero resultados para cualquier término con coma. Confirmado directamente contra la API REST con `curl`. Solución (también documentada por PostgREST): envolver el patrón en comillas dobles dentro de `.or()`.
- Rutas: `despensa/investigar.tsx` (placeholder suelto) se convirtió en `despensa/investigar/index.tsx` (buscador) + `despensa/investigar/[id].tsx` (detalle), mismo patrón que `alimentos/[id]` / `alimentos/editar/[id]`. Único cambio en código ya existente: `FoodDetailScreen.tsx` muestra "Importado del catálogo USDA" cuando `source_reference_food_id` no es null.
- Verificado end-to-end con Playwright (usuario de prueba por SQL): buscar "kale" devuelve correctamente **dos** resultados ("Kale, raw" existe de verdad en Foundation Y en SR Legacy, con fdc_id y valores distintos — se mantienen como dos alimentos separados y trazables, tal cual pide el diseño); abrir el detalle y pulsar "Añadir a mis alimentos" crea la copia con sus 32 columnas correctas (43 kcal, 2,92 g proteína... coincide exacto con el catálogo) y aparece en Alimentos con la nota de origen; pulsar "Añadir" una segunda vez sobre el mismo alimento reutiliza la copia (recuento de filas sin cambiar, no duplica); "Oil, peanut" (sin proteína reportada) muestra el botón deshabilitado, "N/D" en la leyenda y la explicación, sin inventar nada.
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios. `get_advisors` sin avisos nuevos.

## Curación del catálogo a 142 alimentos básicos + traducción al español (2026-09-09)

El usuario probó "Investigar Alimentos" y detectó un problema de fondo de usabilidad: buscar "tomate" devolvía **47 resultados**, todos en inglés (`name_es` seguía vacío en las 6.447 filas — nunca se había rellenado, por precaución excesiva mía al interpretar "no uses IA para generar datos nutricionales" como si también prohibiera traducir nombres, cuando el usuario solo quería decir eso para los *valores*, no para el texto del nombre). Su feedback explícito: *"la app está pensada para uso sencillo y para hispanohablantes... no quiero 30 tipos de tomate, un catálogo de unos 200-300 alimentos básicos debería bastar"*.

- **Proceso seguido, tal como pidió el usuario — proponer antes de tocar nada**: en vez de traducir/borrar directamente, primero investigué a fondo el catálogo real (categoría por categoría, con `SELECT` contra la base de datos, nunca adivinando qué existía) y le presenté una lista curada propuesta, organizada por categoría (frutas, verduras, carnes, pescado/marisco, huevos, lácteos, cereales/arroz/pasta, legumbres, frutos secos/semillas, aceites, tubérculos, harinas/panes), con raw/cocido solo donde tiene sentido culinario real — nunca las 15-40 variantes que reporta USDA para un mismo ingrediente. El usuario aprobó la lista y pidió una comprobación explícita de que cada alimento existe de verdad antes de aplicar nada.
- **Verificación exhaustiva antes de escribir**: de la lista aprobada, confirmé contra la base de datos real el `id` exacto de cada alimento (consultas `SELECT` por categoría/patrón de nombre) — de ~230 conceptos propuestos, **142 tenían una fila real y verificable**; los que no pude confirmar con certeza (p. ej. merluza, trucha, zanahoria cocida, muslo de pollo) se descartaron en vez de adivinar un `id`. Antes de borrar nada, ejecuté primero el `UPDATE` de traducción/categoría sobre los 142 y comprobé que `total_in_list = total_updated` (142 = 142) — es decir, ninguno de los IDs que iba a conservar era inválido, confirmado con datos, no supuesto.
- **Aplicado en una sola transacción SQL** (CTE con `update` seguido de `delete ... where id not in (...)`, referenciando la misma lista de 142 `id`): 1) `name_es` relleno para los 142 alimentos conservados (traducción hecha por mí, revisable, nunca generada por un traductor automático ni una IA de terceros — sigue sin tocar ningún valor nutricional); 2) recategorización de 5 alimentos (patata, boniato, yuca) de `vegetable` a `tubers`, y del cacahuete de `legumes` (clasificación botánica correcta de USDA) a `nuts` (donde lo esperaría un hispanohablante); 3) borrado de los 6.305 alimentos restantes, con `on delete cascade` limpiando automáticamente sus `reference_food_nutrients` (529.485 → 12.571 filas).
- **`scripts/import-usda/translations.es.json` sincronizado con las 142 traducciones aplicadas** (antes vacío) — importante para la reproducibilidad: si en el futuro se vuelve a ejecutar `npm run import:usda` contra los CSV completos, el `upsert` **reinsertaría los 6.305 alimentos borrados** (un `DELETE` no dejó ningún rastro que el importador pueda respetar). Documentado con un aviso explícito en `scripts/import-usda/README.md` con los pasos a seguir si algún día hace falta refrescar el catálogo curado desde una versión más nueva de USDA (reimportar todo + volver a aplicar el filtro de los 142 `fdc_id` de `translations.es.json`).
- Verificado en la app con Playwright: buscar "tomate" → ahora exactamente **1 resultado**, "Tomate crudo"; buscar "pollo" → 3 resultados razonables en español (pechuga cruda/asada, pollo picado). Recuento final confirmado por categoría (28 verdura, 27 fruta, 14 lácteos, 14 pescado, 14 cereales, 13 carne, 11 frutos secos, 11 legumbres, 5 tubérculos, 5 aceites = 142).
- Ningún archivo de código cambió en este bloque (solo datos vía SQL + el fichero de traducciones + documentación) — no aplica verificación de `tsc`/`jest`/`lint`.

## Rediseño de micronutrientes: de chips a barras con nivel alto/bajo/nulo/desconocido (2026-09-09)

Petición de diseño del usuario sobre la pantalla de detalle de un alimento: sustituir los chips con borde (`NutrientChipsRow`) por **barras finas apiladas**, una por nutriente, coloreadas según si la cantidad es alta/baja/nula/desconocida, ordenadas de más a menos informativa (alta → baja → nula/desconocido). Pidió explícitamente recomendaciones de baremo antes de tocar código, y corrigió una vez la semántica: **"nula" es un cero real reportado; "Desconocido" es un dato ausente (NULL)** — no al revés, como yo había propuesto inicialmente.

- `src/features/foods/nutrientThresholds.ts` (nuevo, módulo puro): tabla `NUTRIENT_HIGH_THRESHOLD` por nutriente, con cada cifra trazada a un estándar oficial real — 30% del Valor de Referencia de Nutrientes de la UE (Reglamento (UE) 1169/2011, Anexo XIII) para vitaminas/minerales (el mismo "% VRN" que ya se imprime en cualquier etiqueta europea); el corte "alto" del semáforo de la FSA británica para grasa saturada/azúcar/sal; los umbrales de "alto en fibra"/"alto en omega-3" del Reglamento (CE) 1924/2006 para declaraciones nutricionales; y para sodio (que no tiene VRN propio), el corte de "sal alta" de la FSA convertido a sodio con el factor oficial ×2,5 — usado solo para colorear la barra, nunca guardado ni mostrado como "sal". `null` cuando no existe un estándar oficial (grasas mono/poliinsaturadas, colesterol, cafeína, biotina-B8) — se muestra sin juicio de nivel en vez de inventar un corte. `classifyNutrientLevel(field, value, servingType)`: `null` → `'desconocido'`, `0` → `'nula'`, umbral solo aplicable a `per_100g` (un alimento `per_unit` no tiene gramaje fijo contra el que normalizar, así que siempre cae en `'sin_clasificar'`, neutro).
- `NutrientChipsRow` → renombrado a `NutrientBars` (`src/features/foods/components/NutrientBars.tsx`): ya no es una rejilla de chips que se envuelven, sino una lista vertical de barras de ancho completo, una por cada uno de los 28 nutrientes opcionales (ya no se ocultan los que faltan — mostrar "Desconocido" en vez de omitir la fila es el punto del cambio), ordenadas por `NUTRIENT_LEVEL_ORDER` (`alta, baja, sin_clasificar, nula, desconocido`). Colores del sistema "Vibrante/Enérgico": `accent` (coral) para alta, `accentSecondary` (lima) para baja, `textSecondary`/`placeholder` (grises) para nula/desconocido — la distinción entre estas dos últimas la lleva el texto ("0 g" real vs. "Desconocido"), no el color, ya que ambas son "nada que destacar" visualmente. Gana una prop nueva `servingType: 'per_100g' | 'per_unit'` (antes no existía, porque los chips no distinguían nivel). Actualizados los 3 puntos de consumo (`FoodDetailScreen`, `QuantityScreen` de `meals`, `ReferenceFoodDetailScreen` de `food-catalog`) para pasar `food.serving_type` y el barrel de `foods`.
- Verificado con Playwright contra un usuario y dos alimentos sembrados por SQL: "Lentejas E2E" (`per_100g`, fibra 7,9g/hierro 6,5mg altos, azúcar/vitamina C/sodio bajos, sal en 0, cafeína sin reportar) mostró exactamente el orden y color esperado — fibra y hierro arriba en coral con etiqueta "Alta", azúcar/vitamina C/sodio en lima con "Baja", sal en gris con "0 g" (sin etiqueta, es el cero real), y el resto en gris con "Desconocido" al final. "Huevo E2E" (`per_unit`, hierro 10mg — sería "alta" si fuera per_100g) confirmó que un alimento por unidad muestra sus nutrientes definidos como neutros ("10 mg" sin etiqueta alta/baja), evitando una comparación per-100g engañosa.
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios; sin migraciones nuevas (cambio puramente de UI/cálculo de presentación).

## Nivel intermedio "Normal" en las barras de micronutrientes (2026-09-10)

Petición de seguimiento inmediata: añadir una tercera etiqueta entre "Alta" y "Baja" llamada "Normal", para el rango intermedio que ni destaca por arriba ni por abajo.

- `nutrientThresholds.ts` pasa de un único corte "alto" por nutriente a un par `{ low, high }` (`NUTRIENT_THRESHOLDS`, sustituye a `NUTRIENT_HIGH_THRESHOLD`) — cada `low` verificado contra la misma fuente oficial que ya sustentaba el `high`, nunca calculado a ojo: **grasa saturada/azúcar/sal** usan la fila "LOW" de la Tabla 2 ("Guide to creating a front of pack (FoP) nutrition label", gov.uk/FSA/Dept. of Health, actualizada nov. 2016) — confirmado descargando el PDF real (bajo ≤1,5g/≤5g/≤0,3g, coincide con los "altos" ya existentes de esa misma tabla); **fibra/omega-3** usan el umbral "fuente de" del Reglamento (CE) 1924/2006 (confirmado en el texto de `legislation.gov.uk`: ≥3g fibra, ≥0,3g ALA — exactamente la mitad de sus respectivos "alto en", no una coincidencia sino cómo está redactada la propia norma); **vitaminas/minerales** usan el 15% VRN ("fuente de") frente al 30% VRN ("alto en") ya usado — de nuevo exactamente la mitad; **sodio** deriva su "bajo" del corte de sal de la FSA (0,3g × 2,5 = 120mg), confirmado independientemente por la propia UE (el reglamento define "bajo en sodio" como ≤0,12g/100g — coincide exacto). Nutrientes sin estándar oficial (grasas mono/poliinsaturadas, colesterol, cafeína, B8) siguen sin `low` ni `high` — se quedan en `sin_clasificar`, igual que antes.
- `classifyNutrientLevel`: cuando el valor cae en `[low, high)` devuelve `'normal'` (nuevo nivel); `NUTRIENT_LEVEL_ORDER` pasa a `alta, normal, baja, sin_clasificar, nula, desconocido`.
- Color nuevo en el sistema "Vibrante/Enérgico": `theme.warning` (ámbar — `#E8A33D` claro / `#F5B84F` oscuro), añadido en `src/constants/theme.ts` junto a `accent`/`accentSecondary`/`danger`/`success`. Se eligió ámbar deliberadamente porque replica el semáforo rojo/ámbar/verde de la propia FSA en el que se basan estos cortes — no un color arbitrario. `NutrientBars` usa `theme.warning` para `'normal'`, con su propia etiqueta "Normal" junto al valor (igual que "Alta"/"Baja").
- Verificado con Playwright (alimento sembrado por SQL con fibra 4,5g y sodio 300mg — ambos a propósito en la franja intermedia): "Fibra: Normal 4,5 g" y "Sodio: Normal 300 mg" aparecen en ámbar, ordenados justo después de las altas (Vitamina C, Hierro) y antes de las bajas (Azúcar) — orden y color exactos.
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios; sin migraciones nuevas.

## Reubicación del catálogo USDA bajo el menú "+" de Alimentos (2026-09-10)

Corrección de sitio del usuario: "Investigar Alimentos" nunca estaba pensado como el hogar del buscador USDA — el catálogo debía colgar del botón "+" de la pantalla Alimentos, con un menú de 3 opciones (Alimento Propio / Alimento USDA / Alimento por QR), y "Investigar Alimentos" quedar como placeholder para algo distinto en el futuro.

- **Rutas**: `CatalogSearchScreen`/`ReferenceFoodDetailScreen` se movieron de `despensa/investigar/{index,[id]}` a `despensa/alimentos/usda/{index,[id]}` (título "Catálogo USDA" / "Alimento (USDA)"). `despensa/investigar/index.tsx` pasa a ser un `PlaceholderScreen` (mismo componente compartido que ya usa `AjustesScreen`); se borró `despensa/investigar/[id].tsx` (ya no tiene detalle propio). El enlace "Investigar Alimentos" del menú de Despensa (`DespensaHomeScreen`) sigue apuntando a esa misma ruta, ahora con el placeholder detrás. `CatalogSearchScreen` actualizado para navegar al nuevo `despensa/alimentos/usda/[id]`.
- **`AlimentosHeaderActions`**: el botón "+" ya no navega directo a "Nuevo alimento" — abre un menú modal (mismo patrón visual que `ConfirmDialog`: `Modal` transparente + tarjeta con borde) con tres filas: "Alimento Propio" (icono `create-outline`, va a `alimentos/nuevo`), "Alimento USDA" (icono `globe-outline`, va a `alimentos/usda`) y "Alimento por QR" (icono `qr-code-outline`, sin ruta todavía — muestra el mismo `Alert.alert('Próximamente', ...)` ya usado por el botón de Ayuda, en vez de no hacer nada).
- Verificado con Playwright (usuario de prueba por SQL): el menú de Despensa muestra el placeholder correcto al pulsar "Investigar Alimentos"; el "+" de Alimentos abre el menú de 3 opciones; "Alimento USDA" abre "Catálogo USDA", buscar "tomate" devuelve "Tomate crudo" y su detalle carga con las barras de micronutrientes; "Alimento Propio" abre el formulario "Nuevo alimento" de siempre.
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios; sin migraciones nuevas (cambio de rutas/navegación, no de esquema).

## Toast flotante "en uso" también para Comidas (2026-09-10)

Petición de paridad del usuario: Comidas ya mostraba el mensaje correcto al intentar eliminar una comida en uso en un plan ("Esta comida está en uso en un plan y no se puede eliminar."), pero renderizado como un `ThemedText` inline (empujando el resto del layout) — el mismo problema que tenía Alimentos antes de convertirse en el toast flotante `ErrorBanner`. Se aplicó exactamente el mismo cambio, sin tocar el texto del mensaje ni la lógica de `friendlyDeleteErrorMessage`, que ya era correcta.

- `MealListItem`: pierde su estado local `error` y el `ThemedText` inline; gana un prop `onDeleteError(message)` delegado al padre — mismo patrón que `FoodListItem`.
- `MealsListScreen`: añade el estado `deleteError`, envuelve `Screen` en un `View flex:1` hermano de un `ErrorBanner` a nivel de pantalla, y pasa `onDeleteError={setDeleteError}` a cada `MealListItem` — mismo patrón que `FoodsListScreen`.
- `MealDetailScreen`: sustituye el `ThemedText` inline por el mismo `View flex:1` + `ErrorBanner` hermano de `Screen` — mismo patrón que `EditFoodScreen`.
- Verificado con Playwright (plan + comida sembrados por SQL, comida en uso en el plan): el toast aparece justo encima del menú de tabs sin desplazar la fila de la comida, tanto al borrar desde el listado como desde la pantalla de detalle/edición, y desaparece solo a los ~5s sin dejar hueco.
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios; sin migraciones nuevas (cambio puramente de UI).

## Mismo toast flotante "en uso" para Planes asignados al calendario (2026-09-10)

Misma petición de paridad, ahora para Planes: `PlanListItem` ya mostraba el aviso correcto al intentar eliminar un plan asignado a un día del calendario, pero como `ThemedText` inline. Se aplicó el mismo cambio que a Comidas, con el mismo patrón `ErrorBanner`.

- `PlanListItem`/`PlansListScreen`/`PlanDetailScreen`: mismos tres cambios que en Comidas (prop `onDeleteError` delegado, estado a nivel de pantalla, `View flex:1` + `ErrorBanner` hermano de `Screen`).
- Aprovechado para afinar el texto del aviso, de genérico a específico (igual de concreto que el de Comidas): de "Este plan está en uso y no se puede eliminar." a "Este plan está asignado a un día del calendario y no se puede eliminar." (`friendlyDeleteErrorMessage` en sí no cambió, solo el mensaje que le pasa cada pantalla).
- Verificado con Playwright (plan asignado a un día del calendario sembrado por SQL): mismo toast, mismo formato, en el listado y en el detalle.
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios; sin migraciones nuevas.

## "Programar en calendario": semanas en vez de días + repetición indefinida (2026-09-10)

Petición del usuario: en vez de "repetir durante X días", elegir durante cuántas **semanas** se define (contando la semana actual), y añadir una opción de **repetir indefinidamente** hasta que el usuario la desactive.

Como "indefinidamente" no se puede guardar literalmente (no existe ninguna tabla de reglas recurrentes — `calendar_days` sigue siendo la única fuente de verdad, por diseño explícito desde el bloque 1 de Calendario), antes de tocar código presenté dos decisiones concretas al usuario (`AskUserQuestion`), que aprobó ambas en su opción recomendada:
1. **Mecanismo de "indefinido"**: ventana amplia + auto-extensión — al activarla se materializan de golpe 52 semanas reales de `calendar_days`, y cada vez que se abre el Calendario se comprueba si a algún plan con repetición activa le quedan pocas semanas por delante (menos de 8) y, si es así, se añaden 52 semanas más — así nunca se "acaba" mientras siga activa, sin necesidad de un cron en el servidor (que este proyecto no tiene).
2. **Al desactivar**: se borran los días futuros (desde hoy) que la propia repetición había creado; los días ya pasados no se tocan.

- **`supabase/migrations/0014_plan_recurring_schedules.sql`**: tabla nueva `plan_recurring_schedules` (`user_id`, `plan_id` con `on delete cascade` — a diferencia de `calendar_days.plan_id`, que sigue en RESTRICT, esta fila es la *regla*, no una asignación real: si ya no queda ningún `calendar_days` que la referencie, la regla tampoco significa nada — `weekdays smallint[]`, `active boolean`), con un índice único parcial `where active` que garantiza **una sola repetición activa por plan** (activar una segunda mientras hay una en marcha crearía dos fuentes de auto-extensión escribiendo las mismas filas). `calendar_days` gana una columna nueva `plan_schedule_id` (nullable, `on delete set null`) que etiqueta qué repetición generó cada fila — así desactivarla puede borrar exactamente (y solo) los días futuros que ella creó, nunca uno que el usuario asignara o editara a mano. Políticas RLS de `calendar_days` (`insert`/`update`) ampliadas para verificar también la propiedad de `plan_schedule_id`, mismo patrón defensivo que ya existía para `plan_id`/`routine_id`. `get_advisors` sin avisos nuevos.
- **`schedule.ts`** (módulo puro): nueva `weeksToDays(startDate, weeks)` — la semana 1 cuenta solo los días que quedan de la semana natural (lunes-domingo) que contiene `startDate` (p. ej. empezando en miércoles, "1 semana" son 5 días: miércoles a domingo, no 7), y cada semana adicional es un bloque completo de 7 días; más las constantes compartidas `RECURRING_SCHEDULE_LOOKAHEAD_WEEKS` (52) y `RECURRING_SCHEDULE_TOPUP_THRESHOLD_WEEKS` (8), usadas tanto al activar como en cada auto-extensión. 6 tests nuevos.
- **`api/planRecurringSchedules.ts`** + hooks: `useActiveRecurringSchedule(planId)`, `useStartRecurringSchedule` (crea la regla y materializa las primeras 52 semanas etiquetadas con su id), `useDeactivateRecurringSchedule` (pone `active=false` y borra `calendar_days` desde hoy con ese `plan_schedule_id`), y `useTopUpRecurringSchedules(userId)` — un hook "silencioso" (sin estado de carga/error, sin `useQuery`: un `useEffect` con guarda por `useRef` que se ejecuta una vez por sesión) que recorre las repeticiones activas del usuario y, si a alguna le quedan menos de 8 semanas materializadas, le añade 52 más. Enganchado en `CalendarScreen` al montar. Un fallo a mitad (p. ej. se crea la regla pero falla el primer lote) se autorepara solo: la siguiente auto-extensión ve que no hay ninguna fecha materializada y rellena la ventana completa desde hoy.
- **`PlanScheduleScreen`** rediseñada: un `OptionPicker` "Duración" con dos modos — "Número de semanas" (el `NumericField` de antes, ahora en semanas, con `weeksToDays` para la vista previa y el recuento de conflictos) e "Indefinidamente" (sin campo numérico, con una explicación y un botón "Activar repetición"). Si el plan ya tiene una repetición activa, la pantalla no muestra el formulario — muestra `RecurringScheduleSummary` (días de la semana + botón "Desactivar repetición" con `ConfirmDialog`), igual que `CalendarDayPanel` ya alterna entre "sin planificar" y el resumen asignado según el estado. Así un único punto de entrada ("Programar en calendario") se adapta al estado actual, sin tocar `PlanDetailScreen`.
- Verificado con Playwright (plan sembrado por SQL, hoy jueves): modo semanas con 1 semana y todos los días marcados mostró "Se asignará a 4 días" (jue-dom, cálculo correcto de "contando la actual"); modo indefinido mostró el texto explicativo y "Activar repetición"; tras activar, recargar la pantalla mostró "Repetición indefinida activa · Se repite los días: L, M, X, J, V, S, D"; comprobado en base de datos que se crearon 364 filas de `calendar_days` (52 semanas × 7 días) todas con `plan_schedule_id` relleno; "Desactivar repetición" volvió a mostrar el formulario normal y comprobado en base de datos que las 364 filas se borraron por completo, dejando la regla en `active=false`.
- `tsc --noEmit`, `npx jest`, `npx expo lint` limpios.

## Rehecho: Entrenamiento pasa a ser actividades con gasto calórico por día (2026-09-12)

El usuario corrigió con fuerza el encargo original de Fase 4: el catálogo de Ejercicios + Rutinas (series/repeticiones/peso, espejo de Alimentos→Comidas) **no era lo que había pedido**. La especificación real: Entrenamiento funciona parecido al Calendario de planes nutricionales pero con ejercicios — se asignan **actividades físicas con gasto calórico** (Fuerza / Carrera / Senderismo) a días, el gasto se calcula automáticamente (fórmulas MET oficiales) o se introduce a mano, se suma al gasto energético del día, se pueden asignar varias actividades por día, editarlas/eliminarlas, guardarlas como plantilla reutilizable, y aparecen también en el Calendario general con enlace a la gestión de entrenos.

Se rehizo por completo tras un plan aprobado explícitamente por el usuario (Plan Mode, precedido de 3 agentes Explore en paralelo para inventariar todo lo existente antes de tocar nada) y dos decisiones que el usuario fijó con `AskUserQuestion`: **interpolación lineal** para el MET de carrera entre los dos puntos de tabla más cercanos a la velocidad calculada, y que el gasto quemado **ajusta dinámicamente el objetivo de calorías** del día (no solo informativo).

- **Se eliminó por completo** lo construido en los bloques 1-3 anteriores: `src/features/exercises/`, `src/features/routines/`, las tablas `exercises`/`routines`/`routine_exercises`/`routine_exercise_completions` y los enums `exercise_muscle_group`/`exercise_equipment`, la columna `calendar_days.routine_id`, las rutas `entrenamiento/ejercicios/*`/`entrenamiento/rutinas/*`/`calendario/asignar-rutina.tsx`, y todos los componentes/hooks de `calendar` que dependían de rutinas (`AssignedRoutineSummary`, `RoutinePickerListItem`, `RoutineAssignmentPickerScreen`, `DailyTrainingSection` original, `RoutineExerciseCompletionList`, `useAssignRoutineToDay`, `useCalendarDayRoutineCompletions`, `useToggleRoutineExerciseCompletion`, `api/routineExerciseCompletions.ts`).
- **`supabase/migrations/0015_training_activities.sql`**: desmonta todo lo anterior (orden por FKs) y crea `calendar_activities` + `saved_activities` (plantillas). **Decisión de arquitectura clave**: ninguna de las dos referencia `calendar_days` — tienen su propio `user_id`+`date`, exactamente al mismo nivel que `calendar_days`, en vez de ser una fila hija suya. Dos motivos: 1) `calendar_days` solo existe cuando algo se le asigna, forzar su creación solo para colgar una actividad sería ceremonia sin beneficio; 2) evita un ciclo de imports — `calendar` ya importa de `plans`, y ahora necesita importar de `training` (para el resumen de actividades del día), así que `training` no puede depender de `calendar` en ningún sentido (ni siquiera de `DayStrip`, que se duplicó en pequeño dentro de `training` en vez de compartirse — mismo criterio que ya separa `PlanPickerListItem`/`RoutinePickerListItem`). Enums nuevos: `activity_type` (strength/running/hiking), `activity_calculation_mode` (auto/manual), `activity_effort_level` (light/moderate/intense/easy/demanding/trail — comparte 'moderate' entre Fuerza y Senderismo a nivel de enum, pero el MET/etiqueta se resuelve siempre en TS por `(activity_type, effort_level)`, nunca de forma ambigua, reforzado por un check constraint `..._effort_matches_type`). Bloque RLS estándar de 4 políticas en ambas tablas.
- **`src/features/training/calculations/calories.ts`** (módulo puro + 8 tests): `calculateActivityCalories(met, weightKg, durationMinutes)` — una sola fórmula `(MET-1) × peso × (minutos/60)` compartida por los 3 tipos, tal como especifica el enunciado (solo cambia cómo se obtiene el MET). `STRENGTH_MET`/`HIKING_MET` (tablas fijas dadas por el usuario) y `RUNNING_MET_TABLE` (13 puntos velocidad→MET dados por el usuario) + `interpolateRunningMet(speedKmh)` (interpolación lineal entre los dos puntos que rodean la velocidad real; clamp al extremo si la velocidad cae fuera de la tabla, que no tiene datos más allá). `resolveMet(activityType, params)` centraliza qué tabla/fórmula usar según el tipo — la usan tanto la vista previa en vivo del formulario como la reaplicación de una plantilla guardada, para que nunca diverjan.
- **`schema.ts`**: `activityFormSchema` (zod con `.superRefine`, mismo patrón exacto que `plans/schema.ts` para campos condicionales — minutos+intensidad si Fuerza/Senderismo automático, minutos+distancia si Carrera automático, un único campo de calorías si manual) — precedente real encontrado en el propio repo antes de escribir el nuevo, no inventado desde cero.
- **`mappers.ts`**: `resolveActivityCalories(values, weightKg)` es el único sitio que decide "auto: calcúlalo" vs. "manual: confía en el número" — recibe el peso **actual** del perfil siempre fresco (nunca cacheado), para que un cálculo nunca use un peso obsoleto. `buildActivityInsert`/`buildSavedActivityInsert` construyen las filas a partir de eso.
- **Resto de la feature `training`** (reconstruida desde cero): `api/calendarActivities.ts` + `api/savedActivities.ts` (wrappers finos), hooks (`useDayActivities`, `useActivityDatesInRange` para el punto del day-strip, `useDailyActivitiesTotal` para el ajuste de objetivo, CRUD de actividades y plantillas, `useApplySavedActivity` que **recalcula siempre con el peso actual** al aplicar una plantilla automática — nunca reutiliza el número congelado de la plantilla), y componentes: `EntrenamientoCalendarScreen` (nueva pantalla índice de la tab — ya no es un hub de menú, es un calendario de actividades en espejo del `CalendarScreen` de nutrición, con `TrainingDayStrip` propio + `TrainingDayPanel`), `ActivityForm` (tipo → modo de cálculo, oculta la opción "Automático" si `profile.weight_kg` es `null` → campos condicionales por tipo con vista previa de kcal en vivo → nombre opcional → checkbox "Guardar este entreno para reutilizarlo"), `CreateActivityScreen`/`EditActivityScreen`, `SavedActivityPickerScreen`, y `ActivitiesDaySummary` (el único componente que `calendar` consume — lista de solo lectura + botón "Gestionar entrenamiento" que navega a la tab Entrenamiento con la fecha, sin edición/borrado inline ahí).
- **Cambios en `calendar`** (una sola dirección `calendar` → `training`): `CalendarDayPanel` pierde todo lo de rutinas y su sección "Entrenamiento del día" pasa a ser `<ActivitiesDaySummary userId date />`; `DailyTrackingSection` gana un prop `date` y suma `useDailyActivitiesTotal(userId, date)` al `calories_target` antes de pasarlo a `DailyScoreSummary` — **sin tocar `calculateDailyScore` ni `score.ts`**, el ajuste vive enteramente en ese punto; `DailyScoreSummary` gana un prop opcional `caloriesBurnedToday` para una línea informativa; `DayStrip` sustituye su segundo punto (antes `routine_id`) por `useActivityDatesInRange`.
- **Verificado en vivo** (usuario real registrado a través de la propia UI — sin acceso a SQL, ver nota de entorno abajo): la vista previa del formulario calculó **"Calorías estimadas: 240 kcal"** para Fuerza Moderada (MET 5) + 80kg + 45min, que coincide exacto con el cálculo a mano `(5-1)×80×(45/60)=240` — confirma que la fórmula, el `OptionPicker` de intensidad, y el cableado del formulario funcionan correctamente de extremo a extremo. El resumen de actividades en el panel de Inicio/Calendario ("Todavía no has registrado actividad física este día" + botón "Gestionar entrenamiento") se renderiza correctamente sin errores visibles pese al 404 de fondo (la tabla aún no existe en la base de datos real — ver nota). Al enviar el formulario, el fallo se capturó con el mismo toast `ErrorBanner` flotante que usa el resto de la app ("No se ha podido guardar la actividad. Inténtalo de nuevo."), sin crashear — degradación correcta.
- **Nota de entorno importante**: el MCP de Supabase estuvo desconectado durante todo este bloque, así que **la migración `0015` todavía no se ha aplicado a la base de datos remota** y `database.types.ts` se actualizó a mano (sin poder usar `generate_typescript_types`) reflejando el esquema que la migración creará. Hasta que se reconecte el MCP (o alguien aplique `0015_training_activities.sql` manualmente vía el editor SQL de Supabase) y se regeneren los tipos de verdad, el catálogo de actividades no persistirá datos reales — el error observado en la comprobación de arriba ("relation does not exist") es exactamente ese estado, y no un fallo del código. Queda pendiente además: pasar `get_advisors` sobre la migración real, y borrar el usuario de prueba creado por registro real durante la verificación (no se pudo hacer por SQL).
- `tsc --noEmit`, `npx jest` (59 tests, incluye los 8 nuevos de `calories.ts`), `npx expo lint` limpios.
