EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-side only — used exclusively by scripts/import-usda (Node, never
# bundled into the Expo app). Bypasses RLS: never expose this to the client.
SUPABASE_SERVICE_ROLE_KEY=
